{
  "name": "exitquay",
  "productName": "Exitquay",
  "version": "1.0.0",
  "description": "Exitquay — ProtonVPN country exits for any Docker project",
  "main": "main.js",
  "author": "Exitquay contributors",
  "license": "MIT",
  "homepage": "https://github.com/YOUR_GITHUB_USER/exitquay",
  "repository": {
    "type": "git",
    "url": "https://github.com/YOUR_GITHUB_USER/exitquay.git"
  },
  "private": true,
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "pack": "electron-builder --dir",
    "dist": "electron-builder --publish never",
    "dist:mac": "electron-builder --mac --publish never",
    "dist:win": "electron-builder --win --publish never",
    "dist:linux": "electron-builder --linux --publish never",
    "release": "electron-builder --publish always",
    "postinstall": "electron-builder install-app-deps"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "electron-updater": "^6.6.2"
  },
  "devDependencies": {
    "electron": "^35.1.2",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "app.exitquay.desktop",
    "productName": "Exitquay",
    "copyright": "Copyright © Exitquay contributors",
    "directories": {
      "output": "dist",
      "buildResources": "build"
    },
    "files": [
      "main.js",
      "preload.js",
      "hub.js",
      "updater.js",
      "renderer/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../docker-compose.yml",
        "to": "hub/docker-compose.yml"
      },
      {
        "from": "../countries.conf",
        "to": "hub/countries.conf"
      },
      {
        "from": "../.env.example",
        "to": "hub/.env.example"
      },
      {
        "from": "../bin",
        "to": "hub/bin"
      },
      {
        "from": "../snippets",
        "to": "hub/snippets"
      }
    ],
    "publish": [
      {
        "provider": "github",
        "owner": "YOUR_GITHUB_USER",
        "repo": "exitquay",
        "releaseType": "release"
      }
    ],
    "mac": {
      "category": "public.app-category.utilities",
      "target": [
        {
          "target": "dmg",
          "arch": ["x64", "arm64"]
        },
        {
          "target": "zip",
          "arch": ["x64", "arm64"]
        }
      ],
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "artifactName": "${productName}-${version}-mac-${arch}.${ext}"
    },
    "dmg": {
      "contents": [
        { "x": 130, "y": 220 },
        { "x": 410, "y": 220, "type": "link", "path": "/Applications" }
      ]
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        }
      ],
      "artifactName": "${productName}-${version}-win-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Exitquay",
      "deleteAppDataOnUninstall": false
    },
    "linux": {
      "target": [
        { "target": "AppImage", "arch": ["x64"] },
        { "target": "deb", "arch": ["x64"] }
      ],
      "category": "Network",
      "artifactName": "${productName}-${version}-linux-${arch}.${ext}",
      "synopsis": "ProtonVPN exit switcher for Docker projects",
      "description": "Exitquay switches ProtonVPN exit countries and lets any Docker app attach to the shared tunnel."
    },
    "appImage": {
      "license": "../LICENSE"
    }
  }
}
